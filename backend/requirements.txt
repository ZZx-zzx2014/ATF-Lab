# ============================================================
# ATF Lab - 运行时依赖（Python）
#
# ⚠️ 仅供教学演示（EDUCATIONAL USE ONLY）
# 刻意保持精简：只列真正需要的包。
# 口令哈希使用标准库 hashlib.pbkdf2_hmac，无需 bcrypt/passlib。
# ============================================================

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
PyJWT>=2.8,<3.0
python-multipart>=0.0.9
